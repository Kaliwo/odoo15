<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Inherit the purchase order form view -->
    <record id="view_purchase_order_form_inherit" model="ir.ui.view">
        <field name="name">purchase.order.form.inherit</field>
        <field name="model">purchase.order</field>
        <field name="inherit_id" ref="purchase.purchase_order_form"/>
        <field name="arch" type="xml">
            <xpath expr="//header" position="inside">
                <button name="send_encryption_key" string="Send Encryption Key" type="object" class="oe_highlight" attrs="{'invisible': [('state', '!=', 'purchase')]}"/>
            </xpath>
        </field>
    </record>

    <!-- Menu item for vendors to access the purchase order decryption form -->
    <menuitem id="menu_purchase_order_access"
              name="Enter Access Key"
              action="action_purchase_order_access"
              groups="purchase.group_purchase_user"
              parent="purchase.menu_purchase_root"
              sequence="20"/>
</odoo>
